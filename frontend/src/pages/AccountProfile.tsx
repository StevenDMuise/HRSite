import React from 'react';

const AccountProfile: React.FC = () => {
  return (
    <div>
      <h1>Account / Profile</h1>
      {/* User status, upgrade prompt, trial countdown */}
    </div>
  );
};

export default AccountProfile;
